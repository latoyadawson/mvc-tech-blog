<div class="card">
    <div class="card-header text-white bg-info bg-gradient d-flex justify-content-between">
       <h4><a class="link text-decoration-none" href="/post/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
</div>
