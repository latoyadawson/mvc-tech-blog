{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}

</section>
{{/if}}

<a type="submit" class="btn btn-info btn-lg d-grid gap-2 col-6 mx-auto align-items-center" toggle="offcanvas" href="/dashboard/new">+ New Post</a>

<script src="/javascript/add-post.js"></script>

{{!-- class="link" --}}